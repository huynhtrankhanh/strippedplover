# CoWasm source submodule placeholder

This path is reserved for the CoWasm source submodule used for provenance of
`vendor/python-wasm/dist/python.wasm`.

Initialize it with:

```sh
git submodule update --init vendor/cowasm-source
```

The submodule URL is configured in the repository root `.gitmodules` file.
